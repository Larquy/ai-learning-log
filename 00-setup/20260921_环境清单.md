# 环境清单（2026-09-21）

记录版本号，以后复现出问题能立刻对上是哪一步变了。

## 本地（WSL Ubuntu 20.04，无 GPU）

| 项 | 值 | 备注 |
|---|---|---|
| 系统 | WSL2 · Ubuntu 20.04 | Windows 里跑 Linux |
| GPU | **无 NVIDIA 显卡** | 本地只能跑小实验，训练要走云 |
| Python | 3.11.16 | 无 pip 模块，用 venv / uv 装包 |
| 编译器 | gcc/g++ 9.4.0 | — |
| 其他 | ffmpeg、curl、git | git 还没配用户名/邮箱（待办） |

## 云 GPU（计划）

- 平台：**AutoDL**（待注册）
- 机型：待定，先按**单卡 24G 显存**预算（如 4090 / A10）
- 计费方式：按小时租，跑完就关机（**关机不停止计费，要"关机并停止"**）

## 待办

- [ ] 注册云 GPU 平台，跑通 `nvidia-smi`
- [ ] 建 Python 虚拟环境，装 PyTorch，记录版本号
- [ ] 跑通第一个 PyTorch 脚本（张量运算 → 自动求导）
- [ ] git 配好用户名/邮箱，跑通 push

## 踩的坑

- （还没开始，等踩）
