# 学习路线（五个阶段）

节奏按**每周 8–10 小时**排，每阶段都有通关标准——跑不出东西就不算学完。

总原则：
1. **一条线顺序走**，不要同时开五门课；
2. **每阶段必须有产出物**（能跑的东西），只看视频等于没学；
3. **先上手再补理论**，不需要先啃完大部头。

---

## 阶段 0 · 环境与工具（第 1 周）

| 内容 | 资源 |
|---|---|
| Linux / WSL 基本命令 | `ls cd cp mv grep find`、管道、权限 |
| Git + GitHub | 廖雪峰 Git 教程 · GitHub 官方中文入门 |
| Python 环境 | `venv` / `uv` + `pip`；Jupyter |
| **租云 GPU** | AutoDL（几毛钱一小时） |

**通关**：云 GPU 上跑出 `torch.cuda.is_available() → True`，截图存进仓库。

## 阶段 1 · PyTorch 与深度学习基础（第 2–5 周）

| 类型 | 资源 |
|---|---|
| 主教材 | 李沐《动手学深度学习》· B 站「跟李沐学 AI」 |
| 官方入门 | PyTorch 60 分钟闪电战 · PyTorch 基础篇 |
| 中文文档 | PyTorch 中文文档 |

学：张量 → 自动求导 → 线性回归 → MLP → 训练循环（loss/optimizer/backward/step）→ DataLoader → 保存加载 → 过拟合与正则。

**通关**：不看教程，自己写出 MNIST 训练脚本（训练循环 + 测试准确率）。

## 阶段 2 · Transformer 与大模型（第 6–10 周）

| 顺序 | 资源 |
|---|---|
| ① 论文 | Attention Is All You Need（第一遍只看架构图和结论） |
| ② 视频 | Karpathy《Zero to Hero》，重点 *Let's build GPT: from scratch* |
| ③ 跟着写 | nanoGPT |
| ④ 微调 | LoRA 原论文 + PEFT 文档 |
| ⑤ 模型 | 从 0.5B 级小模型入手（Qwen2.5-0.5B） |

**通关**：① 用 nanoGPT 在小语料上训出能生成文字的字符级模型；② 用 LoRA 微调一个开源小模型跑通。

## 阶段 3 · 数据库与 AI4DB（第 11–16 周）

| 类型 | 资源 |
|---|---|
| 课程 | CMU 15-445（只学：存储、索引、查询执行、优化器） |
| SQL 练习 | SQLBolt |
| 论文 + 代码 | GPTuner（VLDB 2024）仓库 |
| 数学 | 贝叶斯优化（高斯过程、采集函数）——概率统计直接用得上 |

**通关**：跑通 GPTuner demo；能用三句话讲清"数据库旋钮调优为什么可以用贝叶斯优化做"。

## 阶段 4 · 分布式训练（第 17–24 周）

| 主题 | 要点 |
|---|---|
| 并行策略 | 数据并行 / 模型并行 / 流水线并行的取舍 |
| 显存优化 | ZeRO 三阶段、梯度检查点、混合精度 |
| 通信 | AllReduce / Ring AllReduce、通信与计算重叠 |
| 复现目标 | mLoRA（多 LoRA 服务框架）跑通并写一页复现笔记 |

**通关**：多卡（或单卡模拟）跑通一次分布式训练，输出加速比与显存占用数据。

---

## 参考资料（长期备查）

- nanoGPT、mLoRA、GPTuner 三个仓库当"可读的源码"
- 论文阅读顺序：Attention → LoRA → ZeRO → 系统类（VLDB/ICDE 的 LLM 系统工作）
